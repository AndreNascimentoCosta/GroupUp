import 'package:flutter/material.dart';

class ShowOneData extends StatelessWidget {
  const ShowOneData({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        
      ],
    );
  }
}